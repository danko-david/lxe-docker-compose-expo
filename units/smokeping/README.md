# Smokeping

deluxe latency measurement tool. It can measure, store and display latency, latency distribution and packet loss.

## Access

Read only service, you can edit configs in ./data/config. To add host visit
./data/config/Targets